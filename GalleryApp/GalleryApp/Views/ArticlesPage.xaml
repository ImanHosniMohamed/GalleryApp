<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GalleryApp.Views.ArticlesPage"
             xmlns:translate="clr-namespace:GalleryApp.Resources"
             xmlns:vm="clr-namespace:GalleryApp.ViewModel"
             Title="{x:Static translate:AppResources.Articles}" >

    <ContentPage.BindingContext>
        <vm:ArticlesViewModel/>
    </ContentPage.BindingContext>
    
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Search"  IconImageSource="search"/>
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <Grid>
            
        
        <ListView x:Name="ListOfArticles" 
                  HasUnevenRows="True"
                  SeparatorVisibility="None"
                  VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
             <ListView.ItemTemplate>
             <DataTemplate>
                    <ViewCell>
                        <Grid BackgroundColor="White" RowDefinitions="220,Auto,Auto,Auto" RowSpacing="0" 
              HeightRequest="335"
              Margin="25" VerticalOptions="Start">
                            <Image Source="{Binding urlToImage}" HorizontalOptions="FillAndExpand" Aspect="Fill" />
                            <Label Text="{Binding title}" Grid.Row="1" TextColor="#686868" FontSize="15"
                   Margin="15,15,0,0" 
                   />
                            <Label Text="{Binding AuthorBy}" Grid.Row="2" TextColor="#B2ABAB"
                   Margin="15,0,0,0" 
                   />
                            <Label Text="{Binding Date}" Grid.Row="3" TextColor="#B2ABAB"
                   VerticalOptions="End" BackgroundColor="Beige"
                   HorizontalOptions="End" Margin="0,0,15,0"/>

                        </Grid>
                    </ViewCell>
       
        </DataTemplate>
             </ListView.ItemTemplate>
             </ListView>

        <Label Text="Server error ,please try again later" 
               x:Name="NoData"
               IsVisible="false"
               HorizontalOptions="CenterAndExpand"
              Style="{StaticResource DefaultLabel}"  />
        </Grid>
    </ContentPage.Content>
</ContentPage>